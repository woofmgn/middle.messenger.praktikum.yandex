  {{#> AuthLayout }}
    {{#> AuthContainer}}
      {{> AuthTitle text="Регистрация"}}
      {{#> AuthForm}}
        {{> Input label="Почта" name="email"}}
        {{> Input label="Логин" name="login"}}
        {{> Input label="Имя" name="first_name"}}
        {{> Input label="Фамилия" name="second_name"}}
        {{> Input label="Телефон" name="phone"}}
        {{> Input label="Пароль" name="password"}}
        {{> Input label="Пароль (ещё раз)" name="password-check"}}
        {{> Button optClass="button-auth" label="Зарегистрироваться" type="submit"}}
      {{/ AuthForm}}
      {{> Link optionalClass='auth-link' to='#' label='Войти'}}
    {{/ AuthContainer}}
  {{/ AuthLayout }}
