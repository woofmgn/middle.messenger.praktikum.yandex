  {{#> AuthLayout }}
    {{#> AuthContainer}}
      {{> AuthTitle text="Регистрация"}}
      {{#> AuthForm}}
        {{> AuthInput label="Почта" name="email"}}
        {{> AuthInput label="Логин" name="login"}}
        {{> AuthInput label="Имя" name="first_name"}}
        {{> AuthInput label="Фамилия" name="second_name"}}
        {{> AuthInput label="Телефон" name="phone"}}
        {{> AuthInput label="Пароль" name="password"}}
        {{> AuthInput label="Пароль (ещё раз)" name="password-check"}}
        {{> Button optClass="button-auth" label="Зарегистрироваться" type="submit"}}
      {{/ AuthForm}}
      {{> Link optionalClass='auth-link' to='#' label='Войти'}}
    {{/ AuthContainer}}
  {{/ AuthLayout }}
