<div class="chat-header">
  <div class="chat-header__wrapper">
    <div class="chat-header__user-info">
      <img class="chat-header__avatar" src={{avatar}} alt="аватар" class="contact__avatar">
      <h3 class="chat-header__username">{{name}}</h3>
    </div>
    <button class="chat-header__button"></button>
  </div>
  {{#if dropdown}}
    {{> ChatDropdown}}
  {{/if}}
</div>